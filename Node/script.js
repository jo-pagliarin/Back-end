const contas = require('./contas')
// o ./ diz que o arquivo está na pasta onde tem o script
console.log(contas.somar(4,5))

const starwars = require('starwars')

const colors = require('colors')
console.log(starwars().green)
colors

